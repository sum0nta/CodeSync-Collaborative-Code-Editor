// Demo file with intentional syntax errors to test error handling

// Missing closing brace
function demoFunction() {
  console.log("This function is missing a closing brace"
  
// Missing closing parenthesis
if (condition) {
  console.log("Missing closing parenthesis"
}

// Unclosed string literal
const message = "This string is not closed

// Missing semicolon
const x = 5
const y = 10

// Extra closing brace
function anotherFunction() {
  console.log("This function has an extra closing brace")
}
}

// Missing colon in Python-style code (if this were a .py file)
if x > 5
  print("Missing colon")

// Unclosed comment block
/*
This comment block is not closed

// Missing closing bracket
const array = [1, 2, 3, 4, 5

// Valid code to show contrast
function validFunction() {
  const validArray = [1, 2, 3];
  const validString = "This is properly closed";
  console.log("This is valid code");
  return true;
}

